package controller.promotion;

import javafx.event.ActionEvent;
import start.zine.HelloApplication;

public class promotion extends HelloApplication {
    public void back(ActionEvent event) {
        switchWorkStudPage(event);
    }
}
